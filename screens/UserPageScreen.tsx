import React from 'react';

import { View, Text, StyleSheet } from 'react-native';

const UserPageScreen = () => {
	return (
		<View>
			<Text>This is the UserPageScreen</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default UserPageScreen;
