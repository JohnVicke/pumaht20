import React from 'react';

import { View, Text, StyleSheet } from 'react-native';

const ContactScreen = () => {
	return (
		<View>
			<Text>
				This is the Screen where users can find telephonenumbers to Isak
				(ContactScreen).
			</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default ContactScreen;
